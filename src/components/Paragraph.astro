---
import { twMerge } from "tailwind-merge";

interface Props {
  class?: string;
  size?: "base" | "small";
}

const { class: className, size = "base" } = Astro.props;

const sizeClasses = {
  base: "text-base",
  small: "text-sm",
};

const defaultClasses = `${sizeClasses[size]} leading-relaxed text-text-secondary text-pretty`;
const mergedClasses = twMerge(defaultClasses, className);
---

<p class={mergedClasses}>
  <slot />
</p>
