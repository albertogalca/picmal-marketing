---
import { Image } from "astro:assets";
import { getCollection } from "astro:content";
import BaseHead from "../../components/BaseHead.astro";
import BlogNavigation from "../../components/BlogNavigation.astro";
import Footer from "../../components/Footer.astro";
import FormattedDate from "../../components/FormattedDate.astro";
import Header from "../../components/Header.astro";
import { SITE_TITLE } from "../../consts";
import "../../styles/global.css";
import H1 from "../../components/H1.astro";
import Paragraph from "../../components/Paragraph.astro";

const posts = (await getCollection("blog"))
  .filter((post) => post.data.category === "Guide")
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={`Guides - ${SITE_TITLE} | Image Conversion Tips & Best Practices`}
      description="Learn image conversion best practices, file format guides, and optimization tips with our comprehensive guides."
    />
  </head>
  <body class="bg-background overflow-x-hidden">
    <Header />

    <!-- Hero with gradient -->
    <main class="relative z-10 pt-32 pb-20">

      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-16">
          <H1 class="mb-4">Guides</H1>
          <Paragraph class="max-w-2xl mx-auto">
            Best practices, tips, and comprehensive guides for image conversion
            and management.
          </Paragraph>
        </div>

        <!-- Category Navigation -->
        <div class="mb-16">
          <BlogNavigation currentCategory="Guide" />
        </div>

        <!-- Blog Posts Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {
            posts.map((post) => (
              <article class="h-full">
                <a
                  href={`/blog/${post.id}`}
                  class="flex flex-col h-full bg-background-subtle rounded-xl overflow-hidden"
                >
                  <div class="relative aspect-video overflow-hidden bg-background">
                    <Image
                      src={post.data.heroImage || "/images/blog/default.png"}
                      alt={post.data.title}
                      width={800}
                      height={450}
                      loading="lazy"
                      decoding="async"
                      class="w-full h-full object-cover"
                    />
                  </div>
                  <div class="p-4 flex flex-col flex-1">
                    <div class="flex items-center gap-2 mb-3 text-small text-text-secondary">
                      <span class="px-2 py-1 bg-accent/10 text-accent rounded-xl font-medium">
                        Guide
                      </span>
                      <span>•</span>
                      <time datetime={post.data.pubDate.toISOString()}>
                        <FormattedDate date={post.data.pubDate} />
                      </time>
                    </div>
                    <h2 class="text-body font-semibold text-text text-balance mb-2 leading-snug">
                      {post.data.title}
                    </h2>
                    {post.data.description && (
                      <p class="text-small text-text-secondary leading-relaxed line-clamp-3">
                        {post.data.description}
                      </p>
                    )}
                  </div>
                </a>
              </article>
            ))
          }
        </div>

        {
          posts.length === 0 && (
            <div class="text-center py-16">
              <Paragraph>No guide posts found.</Paragraph>
            </div>
          )
        }
      </div>
    </main>

    <Footer />
  </body>
</html>
