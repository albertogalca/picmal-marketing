---
export interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props;
---

<h1
  id={id}
  class={`text-3xl md:text-5xl font-semibold mb-6 leading-tight text-black ${className}`}
>
  <slot />
</h1>
