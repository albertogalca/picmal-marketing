---
import { getEntry, render } from "astro:content";
import BaseHead from "../../components/BaseHead.astro";
import Footer from "../../components/Footer.astro";
import Header from "../../components/Header.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../../consts";
import "../../styles/global.css";
import H1 from "../../components/H1.astro";
import Paragraph from "../../components/Paragraph.astro";

const changelog = await getEntry("blog", "changelog");
if (!changelog) {
  throw new Error("Changelog not found");
}
const { Content } = await render(changelog);
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={`Changelog - ${SITE_TITLE}`}
      description="Stay updated with the latest Picmal updates, new features, and improvements in our changelog."
    />
  </head>
  <body class="bg-background">
    <Header />
    <main class="relative z-10 px-6 pt-32 pb-16">
      <div class="max-w-4xl mx-auto">
        <!-- Blog Header -->
        <div class="mb-12">
          <H1 class="mb-4"> Changelog </H1>
          <Paragraph>
            Latest updates, new features, and improvements to Picmal.
          </Paragraph>
        </div>

        <!-- Changelog Content -->
        <div
          class="prose prose-lg max-w-none prose-headings:text-text prose-p:text-text prose-h1:text-3xl prose-h2:text-2xl prose-h3:text-xl prose-h4:text-lg prose-a:text-accent prose-a:no-underline hover:prose-a:underline prose-img:rounded-lg prose-li:text-text prose-strong:text-text prose-code:text-accent prose-code:bg-gray-100 prose-pre:bg-black prose-blockquote:border-l-accent prose-blockquote:text-text-secondary prose-blockquote:bg-background-subtle *:text-text"
        >
          <Content />
        </div>
      </div>
    </main>
    <Footer />
  </body>
</html>
