---
export interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<h3
  class={`text-lg md:text-xl font-semibold leading-tight text-black ${className}`}
>
  <slot />
</h3>
